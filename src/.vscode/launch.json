{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Win64: Run",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${env:TM_DINO_SDK_DIR}/bin/the-machinery.exe",
            "args": [
                "${workspaceFolder}/../project"
            ]
        },
        {
            "name": "Win64: Attach",
            "type": "cppvsdbg",
            "request": "attach",
            "processName": "the-machinery.exe"
        }
    ]
}